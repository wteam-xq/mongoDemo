extends ../layout

block content
  h3.text-center 用户信息修改
  br
  .container
    .row
      if user && user.name
        form.form-horizontal(name="userform" method="POST" action="/admin/user/update")
          .form-group.hidden
            input(type="text" name="id" value="#{user.id}")
          .form-group
            label.col-md-2.control-label 姓名
            .col-md-8
              input(type="text" name="name" value="#{user.name}").form-control
          .form-group
            label.col-md-2.control-label 年龄
            .col-md-8
              input(type="text" name="age" value="#{user.age}").form-control
          .form-group
            label.col-md-2.control-label 职业
            .col-md-8
              input(type="text" name="job" value="#{user.job}").form-control
          .form-group
            label.col-md-2.control-label 爱好
            .col-md-8
              input(type="text" name="hobby" value="#{user.hobby}").form-control
          .form-group
            .col-md-1.col-md-offset-2
              button(type="submit").btn.btn-primary 提交修改
            .col-md-2
              a(href="/admin/").btn.btn-primary 返回
      else
        .alert.alert-warning 查询用户信息出错